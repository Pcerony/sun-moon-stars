# 视觉验收记录

日期：2026-07-27

## 检查范围

- 视口：320 × 700、390 × 844、460 × 900。
- 流程：欢迎直接发起 NFC、伙伴配对、空首页、区域扫描、任务出现、任务探测、月亮解锁任务详情、星星收藏后返回首页、星空、备用地图。
- 语言：默认中文及英文切换。
- 素材：正式生成插图、实际活动地图、本地字体、Phosphor 圆线图标、正式 Logo 与透明底仪式素材。

## 检查结果

- 首页的信息区、交互区和底部 Dock 分区清晰。
- 产品名称在网页标题、页头、首屏和无障碍名称中统一为 Moving Memories。
- 首屏只使用 Logo、Moving Memories、短句、正式太阳插图和开始按钮，已移除 AI 双人插图。
- 不规则任务贴纸采用错位布局，未与 Dock 或屏幕边缘发生遮挡。
- 图片容器统一为普通圆角矩形和轻量贴纸描边。
- 320px 宽度下，状态数据、任务名称和 Dock 图标均无溢出。
- 页面切换会回到顶部；活动中的任务从首页可以继续进入。
- 普通参与者界面不显示模拟扫描、测试方向或重置控制。
- 欢迎页“开始”会立即进入月亮 NFC 读取；失败后保留必要的重试入口。
- 首次月亮配对使用太阳与月亮相遇并组成伙伴贴纸的独立仪式。
- 初次配对后首页没有小狗和浇花任务，只显示一个区域解锁入口。
- 扫描月亮解锁区域后自动返回首页，并同时出现小狗和浇花两个任务。
- 点击任务直接进入探测器；探测器目标使用任务图形，不再把星星误作寻找目标。
- 探测完成点击“下一步”会先停止方向监听、震动和声音，再进入月亮扫描。
- 第二次月亮扫描解锁任务详情，详情页显示任务插图、简介、积分和星星领取入口。
- 星星扫描使用共同接住并送入上半屏星空的独立仪式。
- 任务探测器以正式太阳插图为中心，任务图形沿目标方向移动；连续接近后自动出现“下一步”。
- 已删除“开始感应”“找到了”、独立完成页和星空页“继续探索”等重复确认。
- 星星收藏后直接返回首页，积分从 0 更新为 10，已完成任务只结算一次。
- 收藏仪式播放时暂停方向传感器触发的页面刷新；连续模拟方向事件不会重置动效 DOM。
- 方向传感器不可用或拒绝权限时显示简短降级提示并开放“下一步”，流程不会被锁死。
- 正式太阳、月亮、星星素材已用于身份、扫描、积分、奖励、Dock、罗盘和星空等语义位置。
- 三张仪式素材具有透明通道，边缘无明显色键残留。
- 减少动态效果模式下，四种 NFC 结果使用 0.7 秒确认画面和 0.22 秒淡出。
- 普通动态模式下，配对、区域和任务解锁仪式持续 4.4 秒，星星收藏持续 5 秒；最终成功画面可见后停留约 1.5 秒。
- 成功音峰值增益提高到 0.14，并使用双音确认；不支持音频的设备仍可依赖画面和震动。
- 动画完成后，原末帧在已准备好的下一页上淡出 650 毫秒，没有瞬间硬切。
- 备用地图使用 `img/map/1120.jpg`，其他页面不把手机地图作为主导航。
- 浏览器控制台检查：0 errors，0 warnings。
- 320 × 700 下区域扫描、任务探测和任务解锁页面均无参与者控件与 Dock 遮挡；390 × 844 完整双任务模拟流程通过。
- 英文在 320 × 700 的任务探测器中无标题或按钮溢出。
- 连续触发 30 次方向事件后，任务解锁页和星星收藏动效 DOM 均保持稳定；两个任务完成后积分为 20。
- Node 测试：26 passed，0 failed。

final result: passed
